server:
  port: 8083
spring:
  application:
    name: feed
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/bloggers
    username: jochman
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
#    todo:make it serverless