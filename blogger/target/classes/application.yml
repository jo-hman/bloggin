server:
  port: 8081
spring:
  application:
    name: bloggers
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/bloggers
    username: jochman
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  zipkin:
    base-url: http://localhost:9411
  rabbitmq:
    addresses: localhost:5672

eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka