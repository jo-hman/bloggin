server:
  port: 8080
spring:
  application:
    name: blog
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/blogs
    username: jochman
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true